# JobNest Morocco - منصة الوظائف المغربية

## معلومات المشروع

- **النوع**: موقع ويب للوظائف في المغرب
- **الاستيراد من**: ملف مضغوط (zip) من مشروع موجود
- **نقطة الدخول**: `src/main.tsx`
- **نظام البناء**: Vite 7.0.0
- **نظام التنسيق**: Tailwind CSS 3.4.17
- **قاعدة البيانات**: Supabase (PostgreSQL)

## الميزات الرئيسية

### الصفحات العامة
- **الصفحة الرئيسية** (`/`) - عرض الوظائف والمقالات الأخيرة
- **صفحة من نحن** (`/about`) - معلومات عن المنصة
- **صفحة اتصل بنا** (`/contact`) - نموذج التواصل
- **سياسة الخصوصية** (`/privacy`) - سياسة الخصوصية
- **الشروط والأحكام** (`/terms`) - الشروط الاستخدام
- **صفحة المقال** (`/article/:slug`) - عرض المقالات

### لوحة التحكم (Admin)
- **تسجيل الدخول** (`/admin/login`) - مصادقة المشرف
- **لوحة التحكم** (`/admin`) - إحصائيات ومعلومات عامة
- **إدارة المقالات** (`/admin/articles`) - إضافة/تعديل/حذف المقالات
- **تحرير المقال** (`/admin/articles/new`, `/admin/articles/:id/edit`)
- **إدارة التصنيفات** (`/admin/categories`) - تصنيفات المقالات
- **إدارة الوظائف** (`/admin/jobs`) - إضافة/تعديل/حذف الوظائف
- **تحرير الوظيفة** (`/admin/jobs/new`, `/admin/jobs/:id/edit`)
- **الإعدادات** (`/admin/settings`) - إعدادات الموقع

## التقنيات المستخدمة

### الأساسية
- React 18.3.1 + TypeScript 5.8.3
- Vite 7.0.0 (أداة البناء)
- Tailwind CSS 3.4.17 (إطار CSS)

### المكتبات
- React Router DOM 6.30.1 (التوجيه)
- Zustand 4.4.7 (إدارة الحالة)
- Supabase JS 2.117.0 (قاعدة البيانات والمصادقة)
- TipTap (محرر النصوص الغني)
- Lucide React (الأيقونات)
- Zod (التحقق من البيانات)

## هيكل المشروع

```
src/
├── api/                  # أكواد API
├── assets/               # الأصول الثابتة
├── components/           # المكونات القابلة لإعادة الاستخدام
│   ├── Header.tsx        # رأس الصفحة
│   ├── Footer.tsx        # تذييل الصفحة
│   ├── CookieConsent.tsx # موافقة الكوكيز
│   └── RichTextEditor.tsx # محرر النصوص الغني
├── layouts/              # مكونات التخطيط
├── pages/                # صفحات التطبيق
│   ├── HomePage.tsx      # الصفحة الرئيسية
│   ├── AboutPage.tsx     # من نحن
│   ├── ContactPage.tsx   # اتصل بنا
│   ├── ArticlePage.tsx   # صفحة المقال
│   ├── AdminLogin.tsx    # تسجيل دخول المشرف
│   ├── AdminDashboard.tsx # لوحة التحكم
│   ├── AdminArticles.tsx # إدارة المقالات
│   ├── AdminArticleEditor.tsx # تحرير المقال
│   ├── AdminCategories.tsx # إدارة التصنيفات
│   ├── AdminJobs.tsx     # إدارة الوظائف
│   ├── AdminJobEditor.tsx # تحرير الوظيفة
│   └── AdminSettings.tsx # الإعدادات
├── store/                # إدارة الحالة
│   └── authStore.ts      # مخزن المصادقة
├── lib/                  # المكتبات المساعدة
│   ├── supabase.ts       # اتصال Supabase
│   ├── content.ts        # إدارة المحتوى
│   └── imageOptimize.ts  # تحسين الصور
├── styles/               # ملفات الأنماط
│   └── article-styles.css # أنماط المقالات
├── App.tsx               # المكون الرئيسي
└── main.tsx              # نقطة الدخول
```

## قاعدة البيانات (Supabase)

### الجداول الرئيسية
- **jobs** - الوظائف المنشورة
- **articles** - المقالات والمدونات
- **categories** - تصنيفات المقالات
- **profiles** - ملفات المستخدمين الشخصية
- **site_settings** - إعدادات الموقع

## ملاحظات للتطوير

- المشروع يستخدم Supabase للمصادقة وقاعدة البيانات
- واجهة المستخدم بالعربية (RTL)
- يتضمن لوحة تحكم شاملة للمشرف
- محرر نصوص غني باستخدام TipTap
- نظام تصنيف للمقالات والوظائف
